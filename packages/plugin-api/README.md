# @ardyn/plugin-api

Plugin API scaffold for future ARDYN tools and capability providers.

Plugins will be manifest-driven and permission-scoped. Phase 3 exports metadata-only registration data and a registry-read capability descriptor; it does not load, install, enable, or execute plugin code.
